{
  "name": "edgesession",
  "license": "SEE LICENSE IN LICENSE",
  "version": "0.0.1-alpha.1",
  "main": "out/index.js",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --outdir=out src/index.ts src/util/index.ts & tsc",
    "pub": "npm run build && npm publish --access public",
    "test": "echo \"Error: use bun test\" && exit 1"
  },
  "devDependencies": {
    "bun-types": "^0.6.0",
    "typescript": "^5.1.3"
  },
  "peerDependencies": {
    "typescript": "^4.0.0 || ^5.0.0"
  }
}