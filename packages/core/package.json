{
  "name": "edgesession",
  "license": "Apache-2.0",
  "version": "0.0.1-alpha.7",
  "main": "lib/index.js",
  "type": "module",
  "scripts": {
    "typecheck": "tsc",
    "build": "bun build --outdir=lib src/index.ts src/util/index.ts & tsc",
    "pub": "rm -rf ./lib tsconfig.tsbuildinfo && npm run build && pnpm publish --access public",
    "test": "echo \"Error: use bun test\" && exit 1"
  },
  "exports": {
    ".": "./lib/index.js",
    "./util": "./lib/util/index.js"
  },
  "files": [
    "lib"
  ],
  "devDependencies": {
    "bun-types": "^0.6.0",
    "typescript": "^5.1.3"
  },
  "peerDependencies": {
    "typescript": "^4.0.0 || ^5.0.0"
  }
}