{
  "name": "@edgesession/adapter-next",
  "main": "lib/index.js",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --outdir=lib src/index.ts & tsc",
    "pub": "npm run build && npm publish --access public",
    "test": "echo \"Error: use bun test\" && exit 1"
  },
  "files": [
    "lib"
  ],
  "devDependencies": {
    "bun-types": "^0.6.0",
    "typescript": "^5.1.3",
    "edgesession": "file:../core"
  },
  "peerDependencies": {
    "typescript": "^4.0.0 || ^5.0.0"
  }
}