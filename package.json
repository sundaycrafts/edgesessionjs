{
  "name": "edgesession",
  "license": "SEE LICENSE IN LICENSE",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --outdir=out ./src/index.ts & tsc",
    "publish": "npm run build && npm publish --access public",
    "test": "echo \"Error: use bun test\" && exit 1"
  },
  "devDependencies": {
    "bun-types": "^0.6.0",
    "typescript": "^5.1.3"
  },
  "peerDependencies": {
    "typescript": "^4.0.0 || ^5.0.0"
  }
}